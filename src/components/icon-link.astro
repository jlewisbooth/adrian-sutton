---
import { Image } from "astro:assets";

const { image, link, width, className } = Astro.props;
---

<a href={link}>
  <Image
    src={image}
    alt=""
    width={width || 50}
    class={className}
    loading="eager"
  />
</a>
